// use experiment00::add_rest_api_scope;
// use std::env::var_os;

// pub fn start_server() -> r2d2::Pool<PostgresConnectionManager> {
//     match var_os("DB_URL") {
//         None => panic!("DB_URL must be set to a valid `postgresql://` address."),
//         Some(db_url) => {

//         }
//     }
// }
